class Solution {
    public int[] runningSum(int[] nums) {
        int len = nums.length, sum = 0;
        int result[] = new int[len];
        for(int i = 0; i < len; i++) {
            sum += nums[i];
            result[i] += sum;
        }
        return result;
    }
}